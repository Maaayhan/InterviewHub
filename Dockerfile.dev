# 前端开发环境 Dockerfile
FROM node:18-slim

WORKDIR /app

# 安装依赖
COPY package*.json ./
RUN npm install --legacy-peer-deps

# 复制源代码（使用 volume mount，这里只是占位）
COPY . .

# 暴露端口
EXPOSE 8000

# 启动开发服务器
CMD ["npm", "run", "start:dev"]


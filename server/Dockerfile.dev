# 后端开发环境 Dockerfile
FROM node:20-slim

WORKDIR /usr/src/app

# 安装依赖
COPY package*.json ./
RUN npm install

# 复制源代码（使用 volume mount，这里只是占位）
COPY . .

# 暴露端口
EXPOSE 7592

# 启动开发服务器
CMD ["npm", "run", "start:dev"]

